package com.michaelflisar.settings.core.enums

enum class CustomLayoutStyle {
    Compact, // global value is not shown für per item settings
    LargeInfo, // global value is shown for per item settings but is not editable
    LargeEditable // global value is shown for per item settings and is editable
}